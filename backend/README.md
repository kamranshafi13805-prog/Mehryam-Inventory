# Backend (Laravel)

This directory contains a Laravel-style API scaffold with REST routes, controllers, and models.

## Next Steps
- Initialize Laravel project (composer install).
- Add migrations mirroring `docs/schema.sql`.
- Implement services, policies, and validation.
- Add auth (Sanctum/Passport) and role middleware.
